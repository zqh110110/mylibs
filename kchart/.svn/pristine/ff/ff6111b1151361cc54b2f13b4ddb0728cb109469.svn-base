 export default class Entity {
     constructor(x, y, title, desc, width, height, option) {
         this.x = x;
         this.y = y;
         this.title = title;
         this.desc = desc;
         this.width = width;
         this.height = height;
         this.option = option;
     }


 }